# Namecheap PostgreSQL connection (update these)
PGHOST=your-pg-hostname
PGPORT=5432
PGDATABASE=your-database-name
PGUSER=your-database-user
PGPASSWORD=your-database-password
# Use 'require' if Namecheap enforces SSL
PGSSL=require

# Optional app settings
OWNER_NUMBER=1234567890
PORT=3000
RESET_TOKEN=your-strong-reset-token